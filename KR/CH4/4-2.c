#include <stdlib.h>
#include <stdio.h>

double atof(char s[]);

void
main(void)
{
	printf("KR 4-2\n");
}

double
atof(char s[])
{
	double val, power;
	int i, sign;
	char dest[128];
	for (i = 0; isspace(s[i]); i++) 
		;
	
}
